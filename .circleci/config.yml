version: 2.1 # use CircleCI 2.1
jobs: # a collection of steps
  build: # runs not using Workflows must have a `build` job as entry point
    working_directory: ~/hello # directory where steps will run
    docker: # run the steps with Docker
      - image: cypress/browsers:chrome67 # ...with this image as the primary container; this is where all `steps` will run
    steps: # a collection of executable commands
      - checkout # special step to check out source code to working directory
      - run:
          name: install-node
          command: 'curl -sL https://deb.nodesource.com/setup_12.x | bash && apt-get install nodejs'
      - run:
          name: update-npm
          command: 'sudo npm install -g npm@latest'
      - run:
          name: update-yarn
          command: 'sudo npm install -g yarn@latest'
      - restore_cache: # special step to restore the dependency cache
          # Read about caching dependencies: https://circleci.com/docs/2.0/caching/
          key: dependency-cache-{{ checksum "yarn.lock" }}
      - run:
          name: install-yarn-wee
          command: yarn install
      - save_cache: # special step to save the dependency cache
          key: dependency-cache-{{ checksum "yarn.lock" }}
          paths:
            - ./node_modules
      - run: # run unit tests
          name: unit-test
          command: yarn test:unit --coverage --json --outputFile=test-results.json
      - store_artifacts: # special step to save test results as as artifact
          # Upload test summary for display in Artifacts: https://circleci.com/docs/2.0/artifacts/ 
          path: test-results.json
          prefix: tests
      - store_artifacts: # for display in Artifacts: https://circleci.com/docs/2.0/artifacts/ 
          path: coverage
          prefix: coverage
      - store_test_results: # for display in Test Summary: https://circleci.com/docs/2.0/collect-test-data/
          path: test-results.json
      - run: # run e2e tests
          name: e2e-test
          command: yarn test:e2e
      - run: # run build
          name: build
          command: yarn build
      - store_artifacts: # for display in Artifacts: https://circleci.com/docs/2.0/artifacts/
          path: dist
          prefix: build